dependencies:
  override:
    - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test; sudo apt-get -y -qq update
    - sudo apt-get -y install qemu-system-ppc qemu-user-static gcc-powerpc-linux-gnu
    - sudo apt-get -y install qemu-system-arm gcc-arm-linux-gnueabi libc6-dev-armel-cross gcc-aarch64-linux-gnu libc6-dev-arm64-cross
    - sudo apt-get -y install libc6-dev-i386 clang gcc-5 gcc-6 valgrind

test:
  override:
  # Tests compilers and C standards
    - |
      [[ "$CIRCLE_NODE_INDEX" == "0" ]] && clang -v && make clangtest && make clean:
        parallel: true
    - if [[ "$CIRCLE_NODE_INDEX" == "0" ]]; then g++ -v; make gpptest && make clean; fi:
        parallel: true
    #- gcc -v; make gnu90test   && make clean
    #- gcc -v; make c99test     && make clean
    #- gcc -v; make gnu99test   && make clean
    #- gcc-5 -v; make gcc5test  && make clean
    #- gcc-6 -v; make gcc6test  && make clean
  # Shorter tests
    - |
      [[ "$CIRCLE_NODE_TOTAL" < "2" ] || [ "$CIRCLE_NODE_INDEX" == "1" ]] && make cmaketest && make clean:
        parallel: true
    - if [[ "$CIRCLE_NODE_TOTAL" < "2" ] || [ "$CIRCLE_NODE_INDEX" == "1" ]]; then make -C lib all && make clean; fi:
        parallel: true
    #- make -C tests dll            && make clean
    #- make -C tests test-zstd      && make clean
    #- make -C tests test-fullbench && make clean
    #- make -C tests test-fuzzer    && make clean
    #- make -C tests test-zstream   && make clean
    #- make -C tests test-invalidDictionaries && make clean
    #- make -C tests test-legacy    && make clean
    #- make -C tests test-symbols   && make clean
    #- make -C tests test-longmatch && make clean
    #- make -C programs zstd-small zstd-decompress zstd-compress && make -C programs clean
    #- make travis-install          && make clean
  # Longer tests
    #- make -C tests test-zstd-nolegacy && make clean
    #- pyenv global 3.4.4; make -C tests versionsTest && make clean
    #- make zlibwrapper         && make clean
    #- gcc -v; make -C tests test32 MOREFLAGS="-I/usr/include/x86_64-linux-gnu" && make clean
    #- make uasan               && make clean
    #- make asan32              && make clean
  # Valgrind tests
    #- CFLAGS="-O1 -g" make -C zlibWrapper valgrindTest && make clean
    #- make -C tests valgrindTest && make clean
  # ARM, AArch64, PowerPC, PowerPC64 tests
    #- make ppctest             && make clean
    #- make ppc64test           && make clean
    #- make armtest             && make clean
    #- make aarch64test         && make clean
